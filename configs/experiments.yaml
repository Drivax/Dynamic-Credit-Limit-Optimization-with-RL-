# Small diagnostics, not a statistically powered policy benchmark.
seed: 42
customers: 6
risk_training_clients: 5000
stress_trials: 200
ppo_timesteps: 4096
ppo_eval_customers: 30
ppo:
  gamma: 0.98
  learning_rate: 0.00025
  n_steps: 128
  batch_size: 64
  n_epochs: 2
  ent_coef: 0.02
  clip_range: 0.2
  policy_kwargs:
    net_arch: [64, 64]
